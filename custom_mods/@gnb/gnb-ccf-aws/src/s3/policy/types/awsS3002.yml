awsS3002:
  title: AWS.S3.002 - Enable Versioning on S3 Buckets
  icon: fal-shield-check
  parent: "#/policy/types/gnbccfAwsS3"
  category: "gnb-ccf#/control/categories/gnbccfData"
  targets:
    - "tmod:@turbot/aws-s3#/resource/types/bucket"
  description: |
    Check if bucket version is enabled
  schema:
    type: string
    enum:
      - Skip
      - Check
      - Per Goliath > AWS
    default: Per Goliath > AWS
