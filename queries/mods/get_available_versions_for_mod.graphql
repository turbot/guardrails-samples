query getAvailableVersionsForMod {
  modVersionSearches(orgName: "turbot", modName: "aws-s3", status: AVAILABLE) {
    items {
      identityName
      name
      versions {
        status
        version
      }
    }
  }
}
